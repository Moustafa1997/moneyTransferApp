/* const { verifyToken } = require('./../../app/services/jwtSign');

const chatController = require('../../app/controllers/socket/socket');
module.exports = (app) => {
  app.use(verifyToken);
  app.get('/api/chats', chatController.getUserChats);
  app.get('/api/chats/search', chatController.searchChats);
  app.get('/api/chats/:chatId/messages', chatController.getChatMessages);
  // app.delete('/api/messages/:messageId', chatController.deleteMessage);
};
 */
